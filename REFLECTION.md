# REFLECTION

## 1. มี​เทสต์​ทั้งหมด​กี่​ตัว​และ​โค้ด​จริง​กี่​บรรทัด​อัตราส่วน​เป็น​เท่าใด

มี Test ทั้งหมด 6 ตัว คือ
- test_multiplication
- test_multiplication_by_two
- test_multiplication_returns_a_new_quantity
- test_equality
- test_grams_are_not_ounces
- test_simple_addition

โค้ดจริงใน `kitchen.py` มีประมาณ 35 บรรทัด
อัตราส่วน Test ต่อโค้ดจริงประมาณ 6:35 

---

## 2. ช่วง​ที่​แถบ​เป็น​สีแดง​นาน​ที่สุด​คือ​เมื่อใด​อะไร​ทำให้​มัน​นาน

ช่วงที่เป็นสีแดงนานที่สุด เกิดตอน A1–A2 เพราะ ตอนเริ่มต้นยังไม่มี `kitchen.py` และยังไม่มี `Quantity` ทำให้ Test ไม่ผ่าน

---

## 3. หา​จุด​ที่​ใช้ Fake It หนึ่ง​จุด​ใช้ Triangulate หนึ่ง​จุด​และ​ใช้ Obvious Implementation หนึ่ง​จุด​เมื่อ​มอง​ย้อนกลับ​ไป​แต่ละครั้ง​เลือก​ได้​เหมาะสม​หรือไม่

- **Fake It:** A1 กำหนดค่าให้ได้ 600 โดยตรง
- **Triangulate:** A2 เพิ่ม Test การคูณด้วย 2 ทำให้ต้องเขียนโค้ดให้รองรับหลายค่า
- **Obvious Implementation:** A3 เขียน `times()` ให้คืน `Quantity` ใหม่โดยตรง

## 4. ยังมี​รายการ​ใด​ค้าง​อยู่​ใน​รายการ​ทดสอบ​รายการ​ใด​ที่จะ​ลงมือทำ​จริง​และ​รายการ​ใด​ที่​ควร​ลบทิ้ง​เพราะ​เป็นการ​คาดเดา​ล่วงหน้า

- การบวก Quantity ที่มีหน่วยเดียวกัน
- การบวก Quantity ที่มีหน่วยแตกต่างกัน เช่น gram กับ ounce
- การแปลงหน่วยโดยใช้ conversion rate
- การนำผลรวมไปคูณ เช่น (200 g + 1 oz) × 2
รายการเหล่านี้ควรลงมือทำจริง เพราะเป็น requirement ของระบบที่ยังไม่ได้ implement

## 5. ดู​เมท็อด Quantity.reduce ถ้า​ออกแบบ​ระบบ​ทั้งหมด​บน​กระดาษ​ก่อนนักศึกษาจะ​เขียน​เมท็อด​นี้​ออกมา​หรือไม่

ถ้าออกแบบก่อนทั้งหมด อาจจะยังไม่เขียน `Quantity.reduce()` เพราะตอนแรกยังไม่รู้ว่าจำเป็น
การทำ TDD ทำให้รู้ว่าควรเพิ่มเมื่อถึงขั้นที่ต้องใช้